# Release status

Current status: **Candidate — source complete, snapshot not yet pinned, no execution recorded**. The pipeline package, the offline test suite (including a tiny random-weight YOLOS run through fine-tuning, evaluation and adapter reload), `MODEL_CARD.md` (MODEL_CARD_SPEC 1.2) and the standalone `E2E` tutorial `tutorials/yolos_detection_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.1) exist. The upstream snapshot is not yet pinned: `MODEL_REVISION` is `"unpinned"`, the manifest records no digests, and the package refuses to load weights until `python tools/pin_snapshot.py` has run. No execution with the pinned weights is recorded in `docs/release-verification.md`, so the repository is not release-grade.
